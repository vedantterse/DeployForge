# Frontend configuration. Copy to `.env.local`.
# NEXT_PUBLIC_* variables are exposed to the browser — never put secrets here.

# Base URL of the DeployForge backend API.
NEXT_PUBLIC_API_URL=http://localhost:8000
